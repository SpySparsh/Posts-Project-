<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dark Theme Form</title>
    <style>
        body {
            background-color: #121212; /* Dark background */
            color: #f5f5f5; /* Light text */
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            height: 100vh;
        }
        .form-container {
            width: 100%;
            max-width: 400px;
            background-color: #1e1e1e; /* Slightly lighter dark background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #ffcc00; /* Highlighted heading */
        }
        label {
            font-size: 14px;
            color: #ccc; /* Slightly dimmed label text */
        }
        input {
            width: 90%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px ;
            border: 1px solid #444;
            border-radius: 5px;
            background-color: #2d2d2d;
            color: #f5f5f5;
            font-size: 16px;
        }
        input::placeholder {
            color: #535252;
        }
        button {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #1e88e5; /* Blue button */
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            margin-top: 15px;
        }
        button:hover {
            background-color: #1565c0; /* Slightly darker blue on hover */
        }
    </style>
</head>
<body>
    <span style>
        <a style="color:#85bff160; font-weight:bold; text-decoration: none; margin-bottom: 20px; font-size: large;" href="/login">Login</a>
    </span>
    <div style="
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 20px;">
    <div class="form-container">
        <h2>Create Account</h2>
        <form action="/register" method="POST">
            <label for="username" style="color: #747272;">Username</label>
            <input type="text" id="username" name="username" placeholder="Enter your username" required>
            
            <label for="password" style="color: #747272;">Password</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required>
            
            <label for="age" style="color: #747272;">Age</label>
            <input type="number" id="age" name="age" placeholder="Enter your age" required>
            
            <label for="email" style="color: #747272;">Email</label>
            <input type="email" id="email" name="email" placeholder="Enter your email" required>
            
            <button type="submit">Submit</button>
        </form>
    </div>
    </div>
</body>
</html>
